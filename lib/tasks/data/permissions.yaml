---
#controller的名字均为程序中controller名称的单数，如AnalysesController为Analysis，必须有对应的model,可以建空model
-
  controller: devices_controller
  actions: [index, new, create, edit, update, show, info, uphold, destroy, update_count, xls_download, parse_excel, query_all]
-
  controller: upholds_controller
  actions: [index, new, create, edit, update, show, destroy, update_count, xls_download, parse_excel, query_all, query_devices]
